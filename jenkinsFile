pipeline {
    agent any
    triggers { cron('0 8 * * *') }
    stages {
        stage('Run suite test at 8AM') {
            steps {
                sh 'mvn clean test -Pgoogle -Dcredentials=prod-credentials-google -Denv=prod-env-google'
            }
        }
    }
}